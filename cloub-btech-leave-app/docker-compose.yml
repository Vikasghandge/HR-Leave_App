version: '3.8'

services:
  mysql:
    image: mysql:8.0
    container_name: cloub-btech-leave-mysql
    restart: always
    ports:
      - "3308:3306"
    environment:
      MYSQL_ROOT_PASSWORD: password
      MYSQL_DATABASE: cloub-btech_leavemanagement
      MYSQL_USER: cloub-btech_user
      MYSQL_PASSWORD: password
    volumes:
      - cloub-btech-mysql-data:/var/lib/mysql
    networks:
      - cloub-btech-network
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 5s
      timeout: 10s
      retries: 10

  app:
    image: ghandgevikas/leave-management:${DOCKER_TAG:-latest}
    container_name: cloub-btech-leave-app
    depends_on:
      mysql:
        condition: service_healthy
    environment:
      SPRING_DATASOURCE_URL: jdbc:mysql://mysql:3306/cloub-btech_leavemanagement?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&autoReconnect=true
      SPRING_DATASOURCE_USERNAME: root
      SPRING_DATASOURCE_PASSWORD: password
      SERVER_PORT: 8095
    networks:
      - cloub-btech-network
    restart: unless-stopped

  nginx:
    build: ./nginx
    container_name: cloub-btech-leave-nginx
    restart: unless-stopped
    ports:
      - "8082:80"
    depends_on:
      - app
    networks:
      - cloub-btech-network

networks:
  cloub-btech-network:
    driver: bridge

volumes:
  cloub-btech-mysql-data:
